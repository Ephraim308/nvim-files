return {
  'nvzone/minty',
  config = function()
    -- You can add any extra configuration for the theme here if needed
  end,
  cmd = { "Shades", "Huefy" },  -- You can configure commands like this if the theme provides them
}

